import React from 'react'class Item extends React.Component{  constructor(props){    super(props);    this.state={      style: {textDecoration: "none"},      done: false    }  }  handleClick = (e) =>{    e.preventDefault();    const { done } = this.state    if( done === false ){      this.setState({        style: {textDecoration: "line-through"},        done: true,      })    }else{      this.setState({        style: {textDecoration: "none"},        done: false,      })    }  }  render(){    const { title , text } = this.props    return(      <li onClick={this.handleClick} style={this.state.style}>        <strong>{title}:</strong> {text}      </li>    );  }}export default Item;
